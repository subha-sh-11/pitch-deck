import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating dev-tools indicator (the "N" badge) in the bottom-left during dev.
  devIndicators: false,
};

export default nextConfig;
