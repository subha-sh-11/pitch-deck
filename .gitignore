README.pdf
docs/

# Secrets — never commit
.env
.env.*
# ...but DO commit the example templates so teammates can bootstrap config
!.env.example
!**/.env.example

# AI-generated build artifacts
/backend/_assets/generated/

# Reference PDFs (binary)
*.pdf
# local-only cloud sync credentials
scripts/cloud.env
