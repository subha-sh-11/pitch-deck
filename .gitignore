README.pdf
docs/

# Secrets — never commit
.env
.env.*
# ...but keep committed example templates
!.env*.example

# AI-generated build artifacts
/backend/_assets/generated/

# Reference PDFs (binary)
*.pdf

# FUSE mount bookkeeping (transient; created when a file is replaced while held open)
.fuse_hidden*